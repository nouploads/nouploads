/**
 * Text diff — web adapter. Single source of truth lives in
 * @nouploads/core/tools/text-diff.
 */
export {
	computeDiff,
	type DiffLine,
	type DiffLineType,
	type DiffResult,
	LARGE_INPUT_THRESHOLD,
	MAX_TEXT_SIZE,
} from "@nouploads/core/tools/text-diff";
