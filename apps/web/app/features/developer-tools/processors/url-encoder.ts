/**
 * URL encoder — web adapter. Single source of truth lives in
 * @nouploads/core/tools/url-encoder.
 */
export {
	decodeUrl,
	encodeUrl,
	isValidUrl,
	parseUrl,
	type UrlParseResult,
} from "@nouploads/core/tools/url-encoder";
