/**
 * Regex tester — web adapter. Single source of truth lives in
 * @nouploads/core/tools/regex-tester.
 */
export {
	MAX_TEST_STRING_SIZE,
	type RegexMatch,
	type RegexResult,
	testRegex,
	validateRegex,
} from "@nouploads/core/tools/regex-tester";
